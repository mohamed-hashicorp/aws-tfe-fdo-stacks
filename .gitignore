# Local .terraform directories (plugin/module cache)
.terraform/

# Terraform state files (should never be committed for Stacks)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Kiro IDE local settings
.kiro/
